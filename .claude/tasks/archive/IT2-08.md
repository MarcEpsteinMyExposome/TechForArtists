# IT2-08: Editor Page Assembly

**Status:** DONE
**Depends on:** IT2-04, IT2-05, IT2-07
**Wave:** 5
**Completed:** 2026-02-06

## Scope

Wire everything together on `/editor` page:
- Form on left, sticky preview + copy button on right
- Two-column grid on desktop (`lg:grid-cols-2`), stacked on mobile
- Auto-create blank signature on first visit if none exist
- Set first signature active if none active

## Files

**Modified:** `app/editor/page.tsx`
**Created:** `__tests__/app/editor/EditorPage.test.tsx` (8 tests)

## Additional Fix: HTML Generation Initials Avatar

Fixed the email HTML generation to include an initials avatar circle when no imageUrl is provided (matching the preview behavior):
- Added `getInitials()` and `generateInitialsAvatarHtml()` to `lib/signature/utils.ts`
- Updated `lib/signature/layouts/horizontal.ts` — adds initials avatar cell when no imageUrl
- Updated `lib/signature/layouts/stacked.ts` — adds initials avatar row when no imageUrl

## Acceptance

- [x] Editor page renders form and preview side by side
- [x] New signature auto-created on first visit
- [x] Preview is sticky on scroll (large screens)
- [x] Responsive: stacks on mobile
- [x] 8 tests passing
- [x] Build succeeds
- [x] Lint passes (0 errors)
