# IT2-02: localStorage Persistence

**Status:** PENDING
**Depends on:** IT2-01
**Wave:** 2

## Scope

Wrap Zustand store with `persist` middleware so signatures survive page refresh.

- localStorage key: `tech-for-artists-store`
- Use `partialize` to only persist `signatures` array
- Include `version: 1` for future migrations

## Files

**Modify:** `lib/store/appStore.ts`
**Tests:** Update `__tests__/lib/store/signatureSlice.test.ts` (clear localStorage in beforeEach)

## Acceptance

- Adding a signature and rehydrating the store retains data
- Store initializes correctly when localStorage is empty
- Only `signatures` array appears in serialized output
